services:
  - type: web
    name: passintel-backend
    env: python
    region: ohio
    buildCommand: "cd passintel-backend/backend && pip install -r requirements.txt"
    startCommand: "cd passintel-backend/backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
